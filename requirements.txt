# Convenience file: installs DIANA (editable) with the notebook extras.
# The authoritative dependency list is in pyproject.toml.
#
#   pip install -r requirements.txt
#
# GPU acceleration (optional) needs the ASTRA Toolbox, installed with conda:
#   conda install -c astra-toolbox -c nvidia astra-toolbox
-e .[notebooks]
